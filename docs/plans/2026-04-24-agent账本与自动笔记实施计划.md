# Agent账本与自动笔记实施计划

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 在不破坏当前会话/工具主链路的前提下，为每个 workspace 引入统一 `agent.db`、记账工具和自动笔记工具。

**Architecture:** 保持现有 `SessionArchive`、`SessionManager`、`ToolRegistry` 外部接口尽量稳定，新增 ledger/notes 存储与工具，底层数据库统一收口到 `agent.db`。

**Tech Stack:** Python, SQLite, FTS5, pytest

---

### Task 1: 统一数据库文件

**Files:**
- Modify: `agent/app.py`
- Modify: `agent/core/loop.py`
- Test: `tests/integration/test_cli_app.py`

1. 写失败测试，断言 testing app 在运行后写入 `workspace/agent.db`。
2. 运行测试，确认当前仍写入 `sessions.db` 而失败。
3. 最小修改 `build_app` / `build_for_test` 的数据库路径为 `agent.db`。
4. 跑测试确认通过。

### Task 2: 新增 ledger 存储与测试

**Files:**
- Create: `agent/memory/ledger_store.py`
- Create: `tests/memory/test_ledger_store.py`

1. 写失败测试覆盖 draft 保存、draft 提交、账目查询与统计。
2. 运行测试确认失败。
3. 最小实现 SQLite 表与方法。
4. 跑测试确认通过。

### Task 3: 新增 notes 存储与测试

**Files:**
- Create: `agent/memory/note_store.py`
- Create: `tests/memory/test_note_store.py`

1. 写失败测试覆盖笔记写入、检索、更新、近期列表。
2. 运行测试确认失败。
3. 最小实现 SQLite 表与方法。
4. 跑测试确认通过。

### Task 4: 暴露 ledger/note tools

**Files:**
- Create: `agent/tools/builtin/ledger_tools.py`
- Create: `agent/tools/builtin/note_tools.py`
- Modify: `agent/tools/registry.py`
- Test: `tests/tools/test_registry.py`

1. 写失败测试，断言默认工具集包含新工具。
2. 跑测试确认失败。
3. 最小实现工具函数与注册逻辑。
4. 跑测试确认通过。

### Task 5: 默认 skill 脚手架

**Files:**
- Modify: `agent/app.py`
- Test: `tests/test_app.py`

1. 写失败测试，断言新 workspace 会生成 `bookkeeping` / `note-taking` skill。
2. 运行测试确认失败。
3. 最小实现 skill 文件脚手架。
4. 跑测试确认通过。

### Task 6: 全量回归

**Files:**
- Test only

1. 跑与 memory/tools/app/web/gateway 相关的目标测试。
2. 跑 `uv run pytest -q`。
3. 确认没有破坏现有多渠道与会话链路。
